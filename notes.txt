// Standings Table Styling
https://dev.to/dcodeyt/creating-beautiful-html-tables-with-css-428l


im on a dev branch..
*check* get nextTag
if Add Player field is not empty when Add Player is clicked
    onchange setup? could allow us to reset the input field?
    send input string and nextTag to parent
    split input first and last and push onto leagueStandings the new item
    nextTag++